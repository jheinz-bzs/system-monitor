# What's New

## System Monitor

Thanks for updating. Here's what changed recently.

### Install with Homebrew (macOS)
- macOS users can now install and update system-monitor with **Homebrew** —
  `brew tap josephheinz/tap && brew install --cask system-monitor`. The
  cask is updated automatically with each release.
