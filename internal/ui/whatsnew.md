# What's New

## System Monitor

Thanks for updating. Here's what changed recently.

### Recordings: compact sessions load from the picker
- The **Recordings** tab's load dialog now shows `.csv.gz` files alongside
  plain CSV, so compact recordings can be opened from the picker instead of
  being invisible. Both formats load the same way.
